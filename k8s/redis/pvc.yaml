apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: payraider
  labels:
    app: payraider
    component: redis
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
