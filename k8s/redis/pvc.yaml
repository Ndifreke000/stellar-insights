apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: stellar-insights
  labels:
    app: stellar-insights
    component: redis
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
