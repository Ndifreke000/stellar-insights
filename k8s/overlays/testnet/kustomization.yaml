apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: payraider-testnet

bases:
- ../../

namePrefix: testnet-

commonLabels:
  environment: testnet

resources:
- namespace.yaml

patches:
- path: backend-patch.yaml

images:
- name: payraider/backend
  newTag: testnet
- name: payraider/frontend
  newTag: testnet

configMapGenerator:
- name: payraider-config
  behavior: merge
  literals:
  - stellar-network=testnet
  - rust-log=info
  - cors-allowed-origins=https://testnet.payraider.com
