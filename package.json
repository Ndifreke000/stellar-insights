{
  "name": "stellar-insights",
  "private": true,
  "version": "0.0.1",
  "description": "Repo-root tooling for stellar-insights: E2E acceptance tests (Playwright) and commit linting.",
  "scripts": {
    "test:acceptance": "playwright test",
    "check:folder-size": "bash scripts/check_folder_size.sh"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "@playwright/test": "^1.48.0"
  }
}
